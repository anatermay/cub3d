
#include "cub3d.h"

//t_game parser_main();

int main(int argc, char **argv)
{
	//t_game  *game;

	if (prestructure_parsing(argc, argv) == FALSE)
		return (1);
	//game = parser_main();
	return (0);
}